# BUG FIX #3: Use relative /api path so all requests go through the Vite proxy.
# Do NOT set this to http://localhost:8000 directly — that bypasses the proxy
# and will cause CORS errors and break API key forwarding.
VITE_API_BASE=/api

