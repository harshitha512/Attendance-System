PORT=5000
NODE_ENV=development

# PostgreSQL - update DB_USER to match your PostgreSQL role (run: psql -c "\du" to list roles)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=attendance_db
DB_USER=postgres
DB_PASSWORD=postgres123

# JWT - CHANGE THIS to a long random string before deploying
JWT_SECRET=ajK83#kL!92jd$9sK@1lPq7xZ
JWT_EXPIRES_IN=8h

# Face service - use http for local dev (https causes TLS errors on localhost)
FACE_SERVICE_URL=http://localhost:8000

# Optional
CORS_ORIGIN=http://localhost:3000
MAX_FILE_SIZE=5242880